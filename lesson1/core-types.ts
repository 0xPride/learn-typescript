// Number
let num: number = 10;// const num = 10;

// String
let message: string = 'Hello World'; // const name = 'Hello World'

// Boolean
let isValid: boolean = true; // const isValid = true

// Objects
const person: {
  name: string,
  age: number
} = {
  name: 'adam',
  age: 20
}

// Arrays
const nums: number[] = [1, 2, 3, 4, 5]; // const nums = [1, 2, 3, 4, 5];

// Tupels
const fullName: [string, string] = ['john', 'smith']

// Enum 
enum Role {
  ADMIN = 0,
  USER = 1,
  GUEST = 2
}
